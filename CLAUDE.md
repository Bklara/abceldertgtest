# CLAUDE.md — ABCELDER Telegram Bot Test

## Overview

Test/prototype repo for ABCELDER Telegram bot integration.

## Workflow

### Planning
1. **Explore** existing code before changes
2. **Plan** in a text file if task is non-trivial
3. After approval: **implement it all**

### Implementation
- Do not add unnecessary comments — keep code clean
- Do not stop mid-task to ask questions

## Rules

- Conventional commits: `feat:`, `fix:`, `docs:`, `chore:`
- No direct push to `main`
- Environment variables via `.env` — never hardcode bot tokens or secrets
